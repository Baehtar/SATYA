python-telegram-bot>=20.0
python-dotenv>=1.0.0
httpx>=0.24.0
pillow>=9.5.0
fastapi>=0.95.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
structlog>=23.1.0
# tests/test_api.py and tests/test_judging_set.py are async; without this plugin
# pytest skips them with "async def functions are not natively supported".
pytest-asyncio>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
google-genai>=2.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
# Publication-date parsing for reverse-image provenance. Small, pure Python,
# and already an indirect dependency of the stack.
python-dateutil>=2.8.2
numpy>=1.24.0
ddgs>=8.0.0
pytesseract>=0.3.10
easyocr>=1.7.0

# Whisper STT runs over the API by default (httpx above — no extra install).
# Only needed for WHISPER_BACKEND=local; pulls in torch and a multi-GB model:
# openai-whisper>=20231117
